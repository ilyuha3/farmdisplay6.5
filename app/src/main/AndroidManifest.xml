<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.nbelov.farmdisplay">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FarmDisplay">
            <activity
                android:screenOrientation="portrait"
                android:name=".ConnectionStringFindAPIActivity"
                android:label="@string/app_name"
                android:theme="@style/Theme.FarmDisplay.NoActionBar" />
            <activity
                android:screenOrientation="landscape"
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppFullScreenTheme"
                />
        <activity
                android:screenOrientation="portrait"
                android:name=".APIQRCodeScanner"
                android:label="@string/app_name"
                android:theme="@style/Theme.FarmDisplay.NoActionBar" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ValidatePermissionsActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FarmDisplay.NoActionBar" />
        <activity
            android:screenOrientation="portrait"
            android:name=".EnregistrementVideoStackActivity"
            android:label="@string/app_name"
            />
            <activity
                android:screenOrientation="portrait"
                android:name=".FindAPIActivity"
                android:label="@string/app_name"
                android:theme="@style/Theme.FarmDisplay.NoActionBar">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>
    </application>
</manifest>